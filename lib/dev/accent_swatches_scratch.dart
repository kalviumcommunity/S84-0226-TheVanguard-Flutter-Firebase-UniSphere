import 'dart:ui';

/// Scratch accent swatches.
///
/// This file is intentionally not imported anywhere, so it has no runtime
/// effect on the project.
abstract final class AccentSwatchesScratch {
  static const Color electricBlue = Color(0xFF00B4D8);
  static const Color brightTeal = Color(0xFF2EC4B6);
  static const Color vividLime = Color(0xFF80ED99);
  static const Color amberPop = Color(0xFFFFBE0B);
  static const Color coralPop = Color(0xFFFF7F51);
  static const Color magentaPop = Color(0xFFFF006E);
}
