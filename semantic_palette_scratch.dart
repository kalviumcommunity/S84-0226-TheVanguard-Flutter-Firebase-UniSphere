import 'dart:ui';

/// Scratch semantic color palette.
///
/// This file is intentionally not imported anywhere, so it has no runtime
/// effect on the project.
abstract final class SemanticPaletteScratch {
  static const Color success = Color(0xFF2A9D8F);
  static const Color warning = Color(0xFFE9C46A);
  static const Color danger = Color(0xFFE76F51);
  static const Color info = Color(0xFF457B9D);

  static const Color surface = Color(0xFFF8F9FA);
  static const Color surfaceMuted = Color(0xFFE9ECEF);
  static const Color textPrimary = Color(0xFF212529);
  static const Color textSecondary = Color(0xFF495057);
}
